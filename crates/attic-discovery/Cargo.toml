[package]
name         = "attic-discovery"
description  = "Attic — repository and file discovery (Bootstrap skeleton)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

# No dependencies during Bootstrap.
# Git-aware ignore walking and domain deps added in Phase 1B.
[dependencies]

[dev-dependencies]
