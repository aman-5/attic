[package]
name         = "attic-storage"
description  = "Attic — SQLite persistence layer (Bootstrap skeleton)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

# No dependencies during Bootstrap.
# SQLite binding and domain deps added in Phase 1A.
[dependencies]

[dev-dependencies]
