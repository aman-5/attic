[package]
name         = "attic-core"
description  = "Attic — pure domain model (Bootstrap skeleton)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

# No dependencies during Bootstrap — domain types are added in Phase 0.
# attic-core must NEVER depend on: tokio, SQLite, rmcp, tree-sitter, notify,
# or any I/O or transport crate.
[dependencies]

[dev-dependencies]
