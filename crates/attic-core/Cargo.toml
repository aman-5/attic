[package]
name        = "attic-core"
description = "Attic — pure domain model: IDs, revisions, snapshots, identities, evidence, retrieval plan"
version.workspace     = true
edition.workspace     = true
rust-version.workspace = true
authors.workspace     = true
license.workspace     = true
repository.workspace  = true

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
thiserror  = { workspace = true }
tracing    = { workspace = true }

# attic-core must NOT depend on: tokio, SQLite, rmcp, tree-sitter, notify,
# or any other I/O or transport crate. That constraint is enforced by this
# Cargo.toml having none of them.

[dev-dependencies]
