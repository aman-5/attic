[package]
name         = "attic-analyzers"
description  = "Attic — language analyzers (Bootstrap skeleton)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

# No dependencies during Bootstrap.
# Analyzer trait, tree-sitter grammars and domain deps added in Phase 1C / Phase 3.
[dependencies]

[dev-dependencies]
