//! attic-evidence — provenance and citation tracking (skeleton).
//!
//! Evidence links retrieved results back to the source locations that
//! justify them. Implementation added in later phases.

#![forbid(unsafe_code)]
#![deny(clippy::all)]

/// Placeholder so the crate compiles during Bootstrap.
pub mod evidence {
    /// Marker type — replaced in Phase 4/5.
    pub struct Evidence;
}

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder_passes() {
        // placeholder — replace with real assertions in Phase 1
    }
}
