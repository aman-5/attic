[package]
name         = "attic-indexing"
description  = "Attic — indexing orchestration (Bootstrap skeleton)"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
authors.workspace      = true
license.workspace      = true
repository.workspace   = true

# No dependencies during Bootstrap.
# Domain deps, tokio, and storage deps added in Phase 1A/1B/1C/2.
[dependencies]

[dev-dependencies]
